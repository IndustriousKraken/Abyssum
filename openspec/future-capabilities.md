# Future Capabilities — moved to `roadmap/`

Deferred-feature intent now lives as individual, status-tracked items under the
repo-root [`roadmap/`](../roadmap/) directory (one file per idea; format defined in
`OCTOPUS.md`). This preserves the same intent — the observing proxy, deep surface
mapping, the recon phone-home box, red-team/C2 interop, embedded/OT/RF surfaces, and
the smaller deferred items — in a form agents check during discuss/audit sessions.

Nothing in `roadmap/` is a v1 commitment. When one of these is built, its behavior
contract goes into `openspec/specs/` as a normal change.
